
(define-class renderer x y z
  (define-method (init)
    sdfsdfsdf))


(define (make-renderer x y z)
  (set! this.x x)
  (set! this.y y)
  (set! this.z z)
  (set! this.init (lambda () asdff)))



function renderer(x, y, z) {
    this.x = y;    
    if(y) this.y = y;
    if(z) this.z = z;

    this.init = function() {}
}
